0.0001 may be a too easy requirement to meet when computing sqrt for small numbers.

I still don't get why it won't work nicely for big numbers.

(define (good-enough? guess x)
  (< (abs (- (improve guess x) guess)) (* 0.0001 guess)))
